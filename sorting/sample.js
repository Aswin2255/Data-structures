/*function quicksort(array){
    if(array.length<2){
        return array
    }
    let left = []
    let right = []
    let pivotic = array[array.length-1]
    for(i=0;i<array.length-1;i++){
        if(array[i]<=pivotic){
            left.push(array[i])
        }
        else{
            right.push(array[i])
        }
    }
    return [...quicksort(left),pivotic,...quicksort(right)]
}
let ar = [10,9,0,3,4,5]
console.log(quicksort(ar))*/

/*function pop(l1){
    if(!l1.top) return 'no element'
    let current = l1
    while(l1.next){

    }
    
}*/

